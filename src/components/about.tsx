import { educationDetails, quickFacts } from "@/data";
import { Reveal } from "@/components/reveal";
import { SectionHeading } from "@/components/section-heading";
export function About() { return <section id="about" className="section-shell"><SectionHeading index="01" label="Profile" title="Curiosity, made operational." description="I like work that asks both how a system functions and how it feels to use." /><div className="about-grid"><Reveal><p className="editorial-quote">“The interface is only the visible edge of a much larger system.”</p><p className="mt-7 max-w-xl leading-7 text-muted-foreground">My practice moves between backend engineering, AI-assisted applications, and interactive environments. I document the connections carefully: requests, states, feedback loops, and the human moments they support.</p></Reveal><Reveal delay={.08} className="fact-panel"><div><p className="chapter-label mb-3">Education</p><h3 className="text-2xl">{educationDetails.degree}</h3><p className="mt-2 text-muted-foreground">{educationDetails.institution} · GPA {educationDetails.gpa}</p></div><ul>{quickFacts.map((fact) => <li key={fact}>{fact}</li>)}</ul></Reveal></div></section>; }
